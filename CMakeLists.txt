PROJECT(distributed_locking)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(distributed_lockingBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

